# Vorlage. Kopiere diese Datei zu `.env.local` und fülle die Werte aus.
# `.env.local` wird durch .gitignore vom Commit ausgeschlossen.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Nur lokal verwendet (BLS-Import-Skript). Niemals im Frontend referenzieren.
SUPABASE_SERVICE_ROLE_KEY=
